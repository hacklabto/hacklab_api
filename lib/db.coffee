# mysql = require('db-mysql')
pg = require('pg')


# connectionString = process.env.DATABASE_URL || 'postgres://localhost:5432/hapi'

# client = new pg.Client(connectionString)
# client.connect()

# module.export = client

